name: CI

on:
  push:
    branches: [main]
    paths:
      - 'image.nix'
      - 'flake.lock'
      - '.github/workflows/ci.yml'
  pull_request:
    paths:
      - 'image.nix'
      - 'flake.lock'
      - '.github/workflows/ci.yml'

jobs:
  verify:
    runs-on: ubuntu-latest
    
    # 1. This block replaces the need for a manually configured token
    permissions:
      id-token: write     # Required for FlakeHub JWT authentication
      contents: read       # Required to checkout code

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v14

      - name: Enable Nix store cache
        uses: DeterminateSystems/magic-nix-cache-action@v14

      - name: Build image
        run: nix build .#coder-workspaces-nix
